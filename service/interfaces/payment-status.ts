export enum PaymentType {
  PACKAGE = 'package',
  EVENT = 'event',
}

export enum PaymentStatus {
  READY = 'ready',
  USED = 'used',
  FAILED = 'failed',
  PENDING = 'pending',
  EXPIRED = 'expired',
}
